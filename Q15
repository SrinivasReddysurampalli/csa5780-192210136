#include<stdio.h>
int main()
{
	int n,arm=0,rem,c;
	printf("enter the number:");
	scanf("%d",&n);
	c=n;
	while(n>0)
	{
		rem=n%10;
		arm=arm+(rem*rem*rem);
		n=n/10;
	}
	if(c==arm)
	{
		printf("armstrong number:");
	}
	else
	{
		printf("not a armstrong number");
	}
	return 0;
}
